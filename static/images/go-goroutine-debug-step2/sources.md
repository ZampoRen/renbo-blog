# go-goroutine-debug-step2 图片来源

- `cover.svg`：作者自制 SVG，技术解释封面图，用于展示 pprof 到三条线排查的关系。
- `cover.png`：由 `cover.svg` 通过 macOS `sips` 转换生成，用作 Hugo 头图，避免发布链路 SVG 兼容风险。
- `inline-01.svg`：作者自制 SVG，正文解释图，用于展示创建路径、取消路径、响应路径三条线。
- `inline-01.png`：由 `inline-01.svg` 通过 macOS `sips` 转换生成，用于正文插图，避免发布链路 SVG 兼容风险。

说明：正文不写图片来源，来源统一记录在本文件，供发布门审核。